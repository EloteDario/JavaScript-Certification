function pairElement (str) {
  const listOfLists = []
  const dic = {"A": "T", "T": "A", "C": "G", "G": "C"}
  for (let i = 0; i < str.length; i++) {
    listOfLists.push([str[i], dic[str[i]]])
  }
  return listOfLists
}
